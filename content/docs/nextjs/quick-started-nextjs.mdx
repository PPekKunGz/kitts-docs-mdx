---
title: 🚀 Quick Start
description: สอนติดตั้งและเริ่มต้นใช้งาน Next.js 15 แบบเข้าใจง่าย
---

## Introduction

Next.js 15 คือเฟรมเวิร์ก React ระดับเรือธงที่ช่วยให้คุณสร้างเว็บได้เร็ว ลื่น และมีโครงสร้างที่จัดการง่ายกว่าเดิม  
ด้านล่างนี้คือคู่มือเริ่มต้นใช้งานแบบกระชับ พร้อมโครงสร้างที่ครบชุด

<Callout title="ข้อดีของ Next.js 15">
  เวอร์ชันใหม่นี้มาพร้อมประสิทธิภาพดีขึ้น, รองรับ Server Actions เสถียรมากขึ้น, และระบบ routing แบบ App Router ที่เป็นมาตรฐานใหม่
</Callout>

---

## 🧱 What You'll Need

<Cards>
  <Card title="Node.js 18.18+ / 20+">
    จำเป็นต้องใช้ Node เวอร์ชันใหม่เพื่อรองรับฟีเจอร์ล่าสุดของ Next.js
  </Card>

  <Card title="Package Manager">
    เช่น npm, pnpm, yarn หรือ bun ก็ได้
  </Card>
</Cards>

---

## 🔧 Check Node Version

```bash
node -v
```

---

## 🚀 Create New Next.js 15 Project

```bash tab="npm"
npm create-next-app@latest
```

```bash tab="pnpm"
pnpm create next-app
```

```bash tab="yarn"
yarn create next-app
```

```bash tab="bun"
bun create next-app
```

ระบบจะถามตัวเลือก เช่น:

- ใช้ TypeScript ไหม  
- เปิด App Router (แนะนำให้เปิด)  
- ใช้ Tailwind CSS หรือไม่  
- ใช้ Turbopack หรือไม่  

---

## 📁 เข้าไปในโปรเจกต์

```bash
cd ชื่อโปรเจกต์ของคุณ
```

---

## ▶️ Start Development Server

```bash tab="npm"
npm run dev
```
```bash tab="pnpm"
pnpm run dev
```
```bash tab="yarn"
yarn run dev
```
```bash tab="bun"
bun run dev
```

เปิดเบราว์เซอร์ที่  
http://localhost:3000

---

## 📂 Project Structure (Next.js 15)

<Files>
  <Folder name="src" defaultOpen>
    <Folder name="app" defaultOpen>
      <File name="layout.tsx" />
      <File name="page.tsx" />
      <File name="globals.css" />
    </Folder>
  </Folder>

  <Folder name="public" defaultOpen>
    <File name="logo.png" />
    <File name="assets/test.png" />
  </Folder>

  <File name="next.config.js" />
  <File name="package.json" />
</Files>

---

## ✏️ สร้างหน้าแรก

แก้ไขไฟล์ `src/app/page.tsx`

```tsx
export default function Home() {
  return <h1>Hello Next.js 15</h1>;
}
```

---

## ➕ เพิ่มหน้าใหม่

สร้างไฟล์:

`app/about/page.tsx`

```tsx
export default function About() {
  return <h1>About us</h1>;
}
```

ตอนนี้เปิดได้แล้วที่:  
http://localhost:3000/about

---

## ⚗️ เปิดฟีเจอร์ทดลอง (Optional)

แก้ `next.config.js`:

```js
const nextConfig = {
  experimental: {
    serverActions: true,
  },
};

export default nextConfig;
```

---

## 🏗️ Build Production

```bash
npm run build
npm start
```

---

## 🌍 Deploy

<Callout title="แนะนำ: Vercel">
  Next.js ถูกสร้างโดยทีม Vercel  
  ซึ่งแปลว่า Deploy ง่ายที่สุดบน Vercel และทำงานเข้ากันดีมาก
</Callout>

ขั้นตอน:

1. Push โปรเจกต์ขึ้น GitHub  
2. เชื่อม GitHub → Vercel  
3. กด Deploy ได้ทันที

---

## 🤔 FAQ

<Accordions>
  <Accordion id="err-node" title="ติดปัญหา Node เวอร์ชันไม่รองรับ?">
    ให้ใช้ Node.js 18.18+ หรือ 20+ เพื่อให้รองรับ Next.js 15
  </Accordion>

  <Accordion id="dynamic-route" title="Route แบบ Dynamic จะช้าไหม?">
    Next.js 15 ทำ Static Optimization ให้อัตโนมัติเมื่อมี generateStaticParams  
    ความเร็วเทียบเท่าหน้า static
  </Accordion>

  <Accordion id="deploy" title="Deploy ที่ไหนดี?">
    แนะนำ Vercel เพราะรองรับฟีเจอร์ Next.js ครบถ้วนที่สุด
  </Accordion>
</Accordions>

---